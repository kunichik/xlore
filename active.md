# Active — live cross-chat board

The current state of every in-flight track, and who owns it. Read this at session start.
Keep it to one screen: it is **state, not history**. When a track is done, move its row to `log.md`.

Update your row when you take or finish work. `Owner` should let another session resume it
(a chat id like `claude --resume <id>`, or the tool + a one-line "where I left off").

| Track | State | Next action | Owner |
|-------|-------|-------------|-------|
| _example: API auth refactor_ | 🟡 in progress | wire refresh-token rotation | claude-code, chat abc123 |

<!-- Replace the example row with your real tracks. Emoji legend: 🟢 done-ish / 🟡 in progress / 🔴 blocked. -->
